# Built sidecar binaries land here from CI per (os, arch). Committed in
# release builds, ignored in source tree so local cargo builds do not
# pollute git status.
/sidecar/

# Rust build artefacts
/sidecar-src/target/

# JS build tooling deps (esbuild). extension.js itself is the BUILT artifact
# and stays committed so install-from-GitHub works without a build step.
/node_modules/

# Release zip
dist/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
